import { EmployeesearchDirective } from './employeesearch.directive';

describe('EmployeesearchDirective', () => {
  it('should create an instance', () => {
    const directive = new EmployeesearchDirective();
    expect(directive).toBeTruthy();
  });
});
